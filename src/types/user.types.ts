/**
 * User-related types
 * Centralized types for user profiles and authentication
 */

export interface UserProfile {
  name: string;
  email: string;
  phone: string;
  province: string;
  canton: string;
  district: string;
  address: string;
}
